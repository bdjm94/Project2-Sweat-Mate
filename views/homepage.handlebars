<<<<<<< HEAD
<div class="dashboard text-center">
    <div class="col-md-6 font-weight-bold" id="current-posts">
        {{#if postStatus}}
        <h3>Current Posts</h3>

        {{#each currentPosts}}
        <div class="row mb-2">
        <div class="col-md-6">
            <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
            <p>{{post.description}}</p>
        </div>
        <div class="col-md-6">
            <a class="btn btn-sm btn-warning" href="/edit/{{post.id}}">Edit</a>
            <button class="btn btn-sm btn-danger delete-post-btn" data-id="{{post.id}}">Delete</button>
=======
<div class="col-auto">
        <h2>Welcome, {{name}}!</h2>
      </div>
    </div>
    
    {{#if blogs.length}}
    <div class="col-md-2 post-list">
      <h3>Your Posts:</h3>
  
      {{#each blogs as |blog|}}
      <div class="row mb-2">
        <div class="col-md-8">
<<<<<<< HEAD:views/homepage.handlebars
          <h4><a href="/blog/{{blog.id}}">{{blog.name}}</a></h4>
=======
          <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
>>>>>>> main
>>>>>>> main:views/dashboard.handlebars
        </div>
        <div class="col-md-4">
            {{!-- <button class="btn btn-sm btn-warning" data-id="{{post.id}}">EDIT</button> --}}
            <button class="btn btn-sm btn-danger" data-id="{{blog.id}}">DELETE</button>
        </div>
      </div>
      {{/each}}
    </div>
    {{/if}}
  </div>

    <form class="form new-post-form">
        <div class="form-post">
          <label for="blog-title">Title</label>
          <input class="form-input" type="text" id="blog-name" name="blog-name" />
        </div>
        <div class="form-group">
          <label for="blog-desc">description:</label>
          <textarea class="form-input" id="blog-desc" name="blog-desc"></textarea>
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Create</button>
        </div>
      </form>
    </div>
  
    
        
    <script src="/js/homepage.js"></script>